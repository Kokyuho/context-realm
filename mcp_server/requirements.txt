# =============================================================================
# ContextRealm — MCP server runtime dependencies
# =============================================================================
# Pinned to a known-good set; bump after a manual smoke test, not on autopilot.
# =============================================================================

# Official MCP Python SDK — provides FastMCP, transport, and protocol code.
mcp>=1.8,<2.0

# ASGI server. uvicorn[standard] pulls in httptools + uvloop for speed.
uvicorn[standard]>=0.30,<1.0

# Already a project-wide dep (used by pipeline/mem0_filter.py and scripts/).
# Re-declared so this image builds in isolation.
httpx>=0.27

# Pydantic is a transitive dep of mcp, but pinned explicitly so v1 and v2
# can't silently swap on us. v1 is what the SDK currently targets.
pydantic>=2.7,<3.0