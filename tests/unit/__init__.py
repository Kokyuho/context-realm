"""Unit tests for ContextRealm.

Unit tests must not require external services. They run on every PR via CI
and on every developer's machine before pushing. Keep them fast (< 1s
total) and free of side effects.
"""
